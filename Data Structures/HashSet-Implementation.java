public static int countWordHashSet (String[] words) {
HashSet<String> mySet = new HashSet<>();
  for(String w: words) { //run time complexity "Linear Complexity"
  mySet.add(words);
  }
  return mySet.size();
}


public static int countWordHashSet (String[] words) {
ArrayList<String> myList = new ArrayList<>();
  for(String w: words) {
    if(!myList.contains(w)){ //run time complexity "Quadratic Complexity"
  mySet.add(words);
    }
  }
  return mySet.size();
}
